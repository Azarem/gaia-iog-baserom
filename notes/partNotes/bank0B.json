{
    "code_0BC90F": "Second logo phase — plays sprite loop #00, clears the tracking flag, and transitions to the title screen.",
    "code_0BC988": "Title screen comet/logo sprite — animated sprite that sweeps across the title, then enables text layers.\nPlays a stationary sprite loop, then a horizontal movement animation, and finally enables BG2+BG3.",
    "code_0BC9A7": "Title screen palette fade — steps palette animation #2D once and terminates.",
    "code_0BC9CF": "Transition to diary menu — clears GFX state, queues scene $FA, syncs VBlank, and fades to black.\nManually sets DP = $0000 for the VBlank call since actor DP points to the actor slot.",
    "code_0BE2F6": "Tab cursor Up — decrement with wraparound (0→3).",
    "code_0BE30F": "Tab cursor Down — increment with wraparound (3→0).",
    "code_0BE32B": "Tab confirm — dispatches to the selected tab handler via SwitchCase on $0D98.",
    "code_0BE354": "Start Journey tab — diary slot selection, save validation, and game load.\nDisplays 3 diary slots with HP/STR/DEF stats. On confirm, validates the SRAM checksum,\nloads event flags, and transitions to scene $E6 (Dark Space) to resume gameplay.\nCamera pan animation (code_0BE527) scrolls to the selected slot's world position.",
    "code_0BE3DF": "Load confirmed — validate SRAM, apply settings, and transition to Dark Space.",
    "code_0BE4EE": "New game start confirmed — apply settings and transition to scene $08 (game start).",
    "code_0BE527": "Camera pan animation — smoothly scrolls the Mode 7 camera to the selected diary slot's\nworld position. Uses Bresenham-style line algorithm to interpolate X/Y camera movement\nover a fixed number of frames ($free101C steps per tick).",
    "code_0BEA55": "Erase Trip Diary tab — select a diary slot and optionally delete it.\nShows slot stats before confirming. Empty slots are erased immediately via ClearSaveSlot.\nOccupied slots show a yes/no confirmation prompt with stat preview.",
    "code_0BEB8B": "Render diary slot stats — for each non-empty slot, reads the saved HP/STR/DEF values,\nconverts them to BCD via sub_0BF1DB, and prints them using the stat display templates.",
    "func_0BC896": "PAL region fallback — loads a palette and displays text-only boot screen via BG3 strings.\nSkips sprite logos entirely. TM = $04 enables BG3 only (text layer, no sprite BGs).",
    "func_0BE2CC": "Main menu re-entry — redraws the 4-option menu and enters the tab selection loop.\nNavigation: Up/Down cycle through 4 tabs ($0D98), A confirms, dispatches via SwitchCase.",
    "func_0BE6BA": "Change Snd/Buttons tab — toggle stereo/mono sound and button layout assignment.\nAfter confirming, saves settings to the active save slot's SRAM and recomputes checksums.",
    "func_0BE8A8": "Copy Trip Diary tab — copy a diary slot's SRAM data to an empty slot.\nFirst checks if all 3 slots are occupied — if so, shows \"Diary not empty\" and returns.\nOtherwise enters source slot selection, then target slot selection (skips occupied slots).\nUses SRAM block copy (JSR $0402) to transfer 512 bytes between slot offsets.",
    "func_0BEC06_noref": "Cursor navigation handler for sub-menus — handles D-pad, L/R paging, A confirm, B/Y cancel.\nReturns carry set + selection index on A press, carry set + $FFFF on cancel.\nAuto-toggles cursor visibility on a 16-frame blink cycle ($18 counter).",
    "func_0BF2A6": "Custom VBlank handler for diary menu — waits for NMI, writes Mode 7 registers,\nreads raw joypad, applies button remap table, and handles auto-repeat timing.\nEach remap variable (remapA through remapSelect) maps a physical button to a\nlogical button mask. The 12-frame auto-repeat threshold enables cursor acceleration.",
    "loc_0BE433": "Load failed (corrupt/empty slot) — show settings arrangement screen instead.\nPresents sound/button config for a new game start.",
    "sub_0BE673": "Apply sound mode and button remap — reads $0B24 (sound) and $0B26 (buttons) from save data.\nSound: $0B24=0 → APUIO0=$91 (stereo), $0B24≠0 → APUIO0=$90 (mono).\nButtons: $0B26=0 → layout A (remap A→$8000, B→$4000, Y→$0040), $0B26≠0 → layout B (no remap).",
    "sub_0BE840": "Read sound/button settings from SRAM for the selected slot.\nExtracts $0B24 (sound mode) and $0B26 (button layout) from the slot's save data.",
    "sub_0BE87C": "Write sound/button settings to SRAM for the selected slot.\nStores $0D90 (sound mode) and $0D8E (button layout) into the slot's save data.",
    "sub_0BEBF9": "Clear VRAM buffer — wrapper that preserves registers across ClearVramBufferFull call.",
    "sub_0BECB1": "Draw cursor highlight — writes tile $202B (highlight arrow) at the cursor's VRAM position.",
    "sub_0BECD9": "Clear cursor — writes blank tile $2040 at the cursor's VRAM position to erase the highlight.",
    "sub_0BECFB": "Clear all cursor positions in a sub-menu — writes blank tiles to every selectable row.\nWraps the cursor index within the valid range using the count from table_0BED3C[0].",
    "sub_0BED64": "Scan all 3 SRAM save slots — validates dual checksums and extracts scene ID, HP, STR, DEF\ninto the diary state block ($0D74–$0D8A). Invalid/empty slots get zeroed entries.\nAlso reads $306000 for the last-used slot number and clamps it to 0–2.",
    "sub_0BF1DB": "BCD number formatter — converts a 16-bit value to packed hundreds:tens:ones format.\nReturns carry clear on success, carry set if value ≥ 1000 (overflow).\nOutput: high byte = hundreds (×$100), low byte = tens (×$10) | ones.\nValues 500+ get pre-subtracted 500 with hundreds starting at 5."
}
